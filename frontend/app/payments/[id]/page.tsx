'use client';import {useEffect,useState} from 'react';import {useParams} from 'next/navigation';import {api} from '../../../lib/api';import {money,dateTime} from '../../../lib/format';import {ErrorState,Loading,PageTitle,Table} from '../../../components/ui';
export default function PaymentDetail(){const {id}=useParams<{id:string}>();const [p,setP]=useState<any>();const [e,setE]=useState('');useEffect(()=>{void api<any>(`/payments/${id}`).then(setP).catch(x=>setE(x.message))},[id]);if(e)return <ErrorState message={e}/>;if(!p)return <Loading/>;return <><PageTitle title="Payment detail" description="Authoritative payment record from the backend."/><div className="cards"><div className="panel"><h3>Payment</h3><div className="financial"><div className="metric"><span>Amount</span><b>{money(p.amount,p.currency)}</b></div><div className="metric"><span>Currency</span><b>{p.currency}</b></div><div className="metric"><span>Status</span><b>{p.status}</b></div></div><Table><tbody>{[['Provider payment ID',p.provider_payment_id],['Internal ID',p.id],['Order / reference',p.provider_order_id],['Provider',p.provider],['Created',dateTime(p.created_at)],['Captured',p.captured?'Yes':'No']].map(([k,v])=><tr key={String(k)}><th>{k}</th><td>{v??'—'}</td></tr>)}</tbody></Table></div><div className="panel"><h3>Safe metadata</h3><pre style={{whiteSpace:'pre-wrap',color:'#8fa2b8',fontSize:11}}>{JSON.stringify(p.raw_data||{},null,2)}</pre></div></div></>}
