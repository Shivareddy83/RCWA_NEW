'use client';import {useEffect,useState} from 'react';import {api} from '../../lib/api';import {Empty,ErrorState,Loading,PageTitle,Table,Badge} from '../../components/ui';
export default function Leads(){const [d,setD]=useState<any>();const [error,setError]=useState('');const load=()=>api<any>('/public/demo-requests').then(setD).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);if(error)return <ErrorState message={error} onRetry={load}/>;if(!d)return <Loading/>;return <><PageTitle title="Demo leads" description="Inbound product interest captured from the public demo form."/>{d.items?.length?<Table><thead><tr><th>Name</th><th>Company</th><th>Email</th><th>Volume</th><th>Status</th><th>Message</th></tr></thead><tbody>{d.items.map((x:any)=><tr key={x.id}><td>{x.name}</td><td>{x.company_name}</td><td>{x.email}</td><td>{x.monthly_transactions}</td><td><select value={x.status} onChange={async e=>{try{await api(`/product/demo-leads/${x.id}`,{method:'PATCH',body:JSON.stringify({status:e.target.value})});await load()}catch(err:any){setError(err.message)}}}><option>NEW</option><option>CONTACTED</option><option>QUALIFIED</option><option>WON</option><option>LOST</option></select></td><td>{x.message||'—'}</td></tr>)}</tbody></Table>:<Empty label="No demo requests yet."/>}</>}
