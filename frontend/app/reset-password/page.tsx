'use client';
import {FormEvent,useState} from 'react';import {useRouter,useSearchParams} from 'next/navigation';import {api,setToken} from '../../lib/api';
export default function ResetPassword(){const router=useRouter();const params=useSearchParams();const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(password!==confirm){setError('Passwords do not match.');return}setBusy(true);setError('');try{const r=await api<any>('/auth/password/reset',{method:'POST',body:JSON.stringify({token:params.get('token')||'',new_password:password})});setToken(r.access_token);router.replace('/')}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="login"><form className="login-card" onSubmit={submit}><div className="brandmark">R</div><h1>Create a new password</h1><p>Use a strong password with at least 10 characters.</p>{error&&<div className="notice error">{error}</div>}<label className="field">New password<input required minLength={10} type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/></label><label className="field">Confirm password<input required minLength={10} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password"/></label><button className="btn primary full" disabled={busy}>{busy?'Resetting…':'Reset password'}</button></form></main>}
