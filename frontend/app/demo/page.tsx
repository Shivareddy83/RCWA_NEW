'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';
export default function Demo(){const [f,setF]=useState({name:'',company_name:'',email:'',monthly_transactions:'100k-500k',message:''});const [sent,setSent]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{await api('/public/demo-request',{method:'POST',body:JSON.stringify(f)});setSent(true)}catch(err){setError(err instanceof Error?err.message:'Unable to submit request.')}}if(sent)return <main className="login"><div className="login-card"><div className="brandmark">R</div><h1>Demo request received</h1><p>We have the details needed to follow up about an RCAA pilot.</p><a className="btn primary full" href="/">Back to RCAA</a></div></main>;return <main className="login"><form className="login-card" onSubmit={submit}><div className="brandmark">R</div><h1>Book an RCAA demo</h1><p>Tell us where reconciliation is creating manual work.</p>{error&&<div className="notice error">{error}</div>}<label className="field">Name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label className="field">Company<input required value={f.company_name} onChange={e=>setF({...f,company_name:e.target.value})}/></label><label className="field">Work email<input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label className="field">Monthly transactions<select value={f.monthly_transactions} onChange={e=>setF({...f,monthly_transactions:e.target.value})}><option>Under 100k</option><option>100k-500k</option><option>500k-2M</option><option>2M+</option></select></label><label className="field">What should we investigate?<textarea rows={4} value={f.message} onChange={e=>setF({...f,message:e.target.value})}/></label><button className="btn primary full">Request demo</button></form></main>}
