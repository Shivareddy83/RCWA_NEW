'use client';
import Link from 'next/link';
const plans=[['Starter','₹9,999','100K transactions / month','For growing finance teams'],['Growth','₹24,999','500K transactions / month','For higher-volume operations'],['Business','₹49,999+','2M transactions / month','For scaled finance operations']];
export default function Marketing(){return <main className="marketing"><section className="hero"><div className="hero-copy"><span className="eyebrow">RCAA · Finance Operations SaaS</span><h1>Reconcile payments. Find the root cause. Close exceptions faster.</h1><p>RCAA connects payment, settlement, refund and bank data into one finance-operations workspace. Deterministic reconciliation establishes the numbers; AI helps explain the exceptions.</p><div className="hero-actions"><Link className="btn primary" href="/signup">Start free trial</Link><Link className="btn secondary" href="/demo">Book a demo</Link></div><small className="muted">No card required to create a trial workspace.</small></div><div className="hero-card"><div className="hero-card-top">EXCEPTION WORKSPACE</div><div className="risk">₹18,420 <span>amount at risk</span></div><div className="mini-grid"><div><b>97.8%</b><span>matched</span></div><div><b>14</b><span>open cases</span></div><div><b>3</b><span>critical</span></div><div><b>4m</b><span>last run</span></div></div><div className="notice">Deterministic financial truth · AI advisory only</div></div></section><section className="marketing-section"><span className="eyebrow">HOW IT WORKS</span><h2>From raw payment data to an explainable case.</h2><div className="product-steps">{[['01','Connect','Upload or ingest payment, settlement, refund and bank data.'],['02','Reconcile','Run deterministic matching with auditable rules.'],['03','Investigate','Open an exception case with evidence and root-cause analysis.'],['04','Resolve','Record the resolution and keep a complete audit trail.']].map(([n,t,d])=><div className="product-step" key={n}><span>{n}</span><h3>{t}</h3><p>{d}</p></div>)}</div></section><section className="marketing-section"><span className="eyebrow">PRICING</span><h2>Start small. Upgrade when transaction volume grows.</h2><div className="pricing-grid">{plans.map(([name,price,limit,desc])=><div className="price-card" key={name}><h3>{name}</h3><strong>{price}<small>/month</small></strong><b>{limit}</b><p>{desc}</p><Link className="btn secondary full" href={`/signup?plan=${name.toUpperCase()}`}>Choose {name}</Link></div>)}</div></section><section className="cta"><div><span className="eyebrow">READY TO TEST IT?</span><h2>Bring one reconciliation problem.</h2><p>Start with a trial workspace, or talk to us about an assisted pilot.</p></div><Link className="btn primary" href="/signup">Create workspace</Link></section></main>}
