'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { api } from '../../lib/api';
export default function ForgotPassword(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [token,setToken]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');setMessage('');setToken('');try{const r=await api<any>('/auth/password/forgot',{method:'POST',body:JSON.stringify({email})});setMessage(r.message);if(r.reset_token)setToken(r.reset_token)}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="login"><form className="login-card" onSubmit={submit}><div className="brandmark">R</div><h1>Reset your password</h1><p>Enter your RCAA account email. We will send a secure, one-time reset link.</p>{message&&<div className="notice">{message}</div>}{error&&<div className="notice error">{error}</div>}<label className="field">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><button className="btn primary full" disabled={busy}>{busy?'Sending…':'Send reset link'}</button>{token&&<div className="notice"><b>Local demo reset token</b><p className="muted">Development environments expose the token so you can test the complete flow.</p><Link className="link" href={`/reset-password?token=${encodeURIComponent(token)}`}>Continue to password reset</Link></div>}<p className="muted"><Link className="link" href="/login">Back to sign in</Link></p></form></main>}
